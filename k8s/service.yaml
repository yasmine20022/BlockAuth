apiVersion: v1
kind: Service
metadata:
  name: blockauth
  labels:
    app: blockauth
spec:
  type: NodePort
  selector:
    app: blockauth
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30080
